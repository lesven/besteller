{% extends 'base.html.twig' %}

{% block title %}Bereits eingereicht - Besteller{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card border-warning">
                <div class="card-header bg-warning text-dark text-center">
                    <h1 class="h3 mb-0">
                        <i class="fas fa-exclamation-triangle"></i> Bereits eingereicht
                    </h1>
                </div>
                <div class="card-body text-center">
                    <h4>Hallo {{ name }}!</h4>
                    <p class="text-muted">
                        Für die Mitarbeiter-ID <strong>{{ mitarbeiterId }}</strong> wurde die Stückliste 
                        <strong>{{ checklist.title }}</strong> bereits ausgefüllt und eingereicht.
                    </p>
                    
                    <div class="alert alert-warning mt-4">
                        <i class="fas fa-info-circle"></i>
                        <strong>Hinweis:</strong> Pro Person kann die Stückliste nur einmal ausgefüllt werden.
                    </div>
                    
                    <hr>
                    
                    <p class="mb-0">
                        <small class="text-muted">
                            Bei Fragen oder Änderungen wenden Sie sich bitte an die IT-Abteilung.
                        </small>
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
